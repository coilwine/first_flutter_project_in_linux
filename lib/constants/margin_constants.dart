import 'package:flutter/material.dart';

class MarginConstants {
  static MarginConstants instance = MarginConstants._init();
  MarginConstants._init();
  final marginNormal = const EdgeInsets.all(16.0);
}
